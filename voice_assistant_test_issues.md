# Voice assistant test issues

Дата: 2026-09-14

## Открытые расхождения

- `03_shampoo_kokoro.mp3` — Whisper: `אפשר שימפו ומרכך בבקשה.`; исходный текст: `אפשר שמפו ומרכך בבקשה`. Классификация: `request` — верная.
- `17_wifi_problem_kokoro.mp3` — Whisper распознал фразу, но классификатор вернул `request` вместо ожидаемого `info` / `wifi`.
- `29_toilet_flush_kokoro.mp3` — Whisper: `הנגרה בשירותים לא מפסיקה לרוץ.`; исходный текст: `הניאגרה בשירותים לא מפסיקה לרוץ`. Классификация: `request` — верная.
- `33_spa_massage_kokoro.mp3` — Whisper: `אני רוצה להזמין ניסוי.`; исходный текст: `אני רוצה להזמין עיסוי`. Классификация всё равно вернула `info` / `spa_transfer` — верная.
- `35_human_agent_kokoro.mp3` — Whisper: `אפשר לדבר עם נציג אנושה.`; исходный текст: `אפשר לדבר עם נציג אנושי`. Классификация: `transfer` — верная.
- `39_smell_and_leak_kokoro.mp3` — Whisper: `יש ריח רע בחדר וגם נזילה מתחת לקיור.`; исходный текст: `יש ריח רע בחדר וגם נזילה מתחת לכיור`. Классификация: `request` — верная.

## Исправлено и повторно проверено

- В режиме transfer `נכון` теперь распознаётся как согласие (`transfer`), а `לא נכון` — как отказ (`end`).
- Вопрос про завтрак во время ожидания transfer не сбрасывает режим: воспроизводится `transfer_answer_required`.
- Смешанный запрос `אני רוצה מגבות ולשאול מתי יש ארוחת בוקר` возвращает `separate` и воспроизводит `separate_request_or_question`.
