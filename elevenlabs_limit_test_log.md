# Проверка лимитов ElevenLabs — 2026-09-15

- PASS — временный лимит `10` символов за звонок: после ответа длиной 50 символов запущен `stop_request_and_changing_line.mp3`, напечатано `доделать логику когда программа будет готова`, комнате установлен Kokoro на 6 часов, процесс завершился.
- PASS — временный лимит отеля `350` символов в день (счётчик отеля был `343`): следующий ответ вызвал тот же стоп-сценарий и завершение. Новый тестовый номер комнаты сразу получил Kokoro до конца дня.
- PASS — ложная проверка пустого баланса: запуск вывел `ElevenLabs: токены закончились → текущий звонок будет завершён`, затем `ELEVENLABS: В ELEVENLABS НЕТ ТОКЕНОВ`, проиграл стоп-сценарий и завершился.
- PASS — после восстановления настоящей проверки ElevenLabs API вернул `1225/10000`; обычный запуск выбрал ElevenLabs.

Восстановлено после теста:

- `MAX_CHARS_PER_CALL = 250`
- `MAX_CHARS_PER_DAY = 5000`
- настоящая проверка баланса ElevenLabs
- временные флаги дневного Kokoro, пустого баланса и тестовые номера комнат удалены из `room_status.json`.

Проверка безлимитности Kokoro: в его функции нет учёта `chars_this_call` и нет вызова `update_room_chars`; лимиты применяются только в пути ElevenLabs.
